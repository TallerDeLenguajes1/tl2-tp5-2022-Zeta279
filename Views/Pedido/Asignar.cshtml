@{
    ViewData["Title"] = "Asignar pedido";
}
@model PedidoModel

<section>
    <form method="post">
        <input type="number" id="idp" name="idp" value="@Model.Nro" hidden>
        <select id="idc" name="idc">
            @foreach(var cadete in DataModel.CadeteVList)
            {
                <option value="@cadete.CadeteId">@cadete.nombre</option>
            }
        </select>
        <input type="submit" value="Asignar al pedido">
    </form>
</section>